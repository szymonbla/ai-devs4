# AI_devs 4 — Lesson 1 Notes (Socratic Edition)

## 1. How LLMs Work

A language model is a function that takes text as input and predicts what token (word fragment) comes next. It repeats this in a loop until the answer is complete.

Because of this, it's **non-deterministic** — the same question can get a different answer each time. It's also **autoregressive** — every token generated so far influences the next one.

The API is **stateless**: the model has no memory between calls. You must send the full conversation every time.

> **Check yourself:**
> - Why can't you just send the new message and skip previous ones?
> - If the model is non-deterministic, what does that mean for using it in production code?
> - A token is not always one word. Why does this matter when you're paying per token?

---

## 2. Tokens and Context Window

Tokens are numeric IDs representing fragments of text. The model never sees "letters" — only numbers. The **context window** is how many tokens can be processed at once (input + output combined).

Polish text uses ~50–70% more tokens than English for the same content. That means slower responses and higher costs.

> **Check yourself:**
> - If you're building a Polish-language chatbot, what's the practical consequence of this tokenization difference?
> - What happens when a conversation gets so long it exceeds the context window?
> - Why do you think special tokens like `<|im_start|>` are needed?

---

## 3. Controlling Behavior with Code

The key insight of this lesson: **controlling an LLM = managing what's in the context**. Code lets you:
- Decide what to include in each request
- Chain multiple LLM calls (output of one → input of next)
- Route requests (classify first, then respond differently based on category)

> **Check yourself:**
> - You want to build a customer support bot that answers differently for billing vs. technical questions. How would chaining two LLM calls help here?
> - What's the tradeoff of chaining multiple calls instead of doing everything in one?

---

## 4. Structured Outputs (JSON Schema)

LLMs return text. But apps need structured data (JSON). **Structured Outputs** forces the model to respond in a schema you define.

Two things to understand:
1. **Structure** — in `strict` mode, guaranteed. The JSON shape is exactly what you asked for.
2. **Values** — generated by the model. The field names and descriptions in your schema directly influence what values get produced.

Always include a "neutral" or "unknown" option when doing classification — it reduces hallucinations because you're not forcing the model to pick a category when it can't.

The **order of fields matters** — earlier fields influence later ones. Put "reasoning" before "answer".

> **Check yourself:**
> - You're classifying emails as urgent/not urgent. What third option should you add to your schema and why?
> - Your schema has a field `x` that means "the deadline". Will the model fill it correctly? What's wrong?
> - Why would you put a `reasoning` field before a `conclusion` field in your schema?

---

## 5. Prompt Design

A system prompt is not just instructions — it's a **space** you define for the model to operate in. Key elements:

| Element | Purpose |
|---|---|
| **Identity** | Sets general context/associations |
| **Limits** | Restrict unwanted behavior (not guaranteed!) |
| **Style** | Tone, verbosity, format |
| **Constraints** | Conditional rules (e.g. must find item before editing it) |
| **Security** | Reduce jailbreak risk |
| **Calibration** | Specific formatting rules |

> **Check yourself:**
> - You write: "Always respond in formal Polish." Is this identity, style, or constraint?
> - Can system prompt instructions be bypassed? What does this mean for security-critical actions?
> - Why might making the assistant "verbose" actually hurt performance?

---

## 6. "Generalizing Generalizations" (The Most Important Prompt Technique)

When you see the model making a mistake, **don't** add a specific rule to fix that specific case. Instead, define a **general thinking process** that covers the whole category of mistakes.

**Bad**: "When the user says 'meeting', use the `add_event` tool, not `add_task`."
**Good**: "Before using any tool, think aloud about which tool fits, rate your confidence 1–10, and ask the user to clarify if confidence is below 7."

The second rule works for any tool, any task, any configuration.

> **Check yourself:**
> - Why is the "bad" approach risky in production?
> - Can you think of a case in your own work where you solved a specific problem but the same category of problem kept returning?
> - What does "generalizing a generalization" have in common with writing good unit tests?

---

## 7. Few-shot Learning

If you want the model to follow a specific pattern (e.g. a custom classification logic), describing it in words isn't always enough. Show it **examples** of input→output pairs. The model will recognize the pattern and apply it.

Modern approach: instead of putting all examples in the system prompt, give the model a **tool to load examples on demand**. This keeps the system prompt focused.

> **Check yourself:**
> - You have 50 examples of good customer responses. Should you put all 50 in the system prompt? Why or why not?
> - What's the difference between the model "knowing" something from training vs. "learning" from examples in the prompt?

---

## 8. Storing Prompts

4 ways, from simplest to most powerful for agents:

1. **Inline** — prompt written directly in code. Simple, but hard to monitor/update.
2. **Separate files** — prompt in `.txt`/`.md` file with variables. More flexible.
3. **External system** (e.g. Langfuse) — version control, monitoring, no redeploy needed.
4. **Markdown + YAML frontmatter** ← recommended for agents — the model itself can read and modify these files as tools.

> **Check yourself:**
> - You need to quickly fix a prompt in production without redeploying. Which approach enables this?
> - Why would an agent need to be able to read its own prompt file?

---

## 9. Model Selection

No model is universally best — only best **for a given task**. Common strategies:

- **One model**: simplest
- **Main + fast/cheap fallback**: expensive model for hard tasks, cheap for easy
- **Main + specialists**: different models for different domains
- **Team of small models**: rare, complex — decompose tasks + vote on answers

To stay current: follow OpenRouter, Replicate, and company Twitter accounts. Evaluate models on your own tasks.

> **Check yourself:**
> - You're building a chatbot that summarizes legal documents (hard) and greets users (easy). Which strategy fits?
> - Why is "vendor lock-in" a risk when choosing an AI provider?

---

## 10. DB Structure (Chatbot vs Agent)

**Chatbot**: just `conversations` + `messages` tables.

**Multi-agent system** needs:
- `sessions` — one per user interaction, tracks lead agent
- `agents` — one per agent instance, tracks task, status, who assigned it
- `items` — individual steps: messages, tool calls, file attachments

> **Check yourself:**
> - Why isn't a simple `messages` table enough for a multi-agent system?
> - What does "agent status" allow you to do that a simple chatbot can't?

---

## 11. Lesson 1 Task: People

**Context**: Find people who may be organizing nuclear fuel transports.

**Your steps**:
1. Fetch `people.csv` from the hub (insert your API key in the URL)
2. Filter: gender=M, born in Grudziądz, age 20–40 in 2026 (born 1986–2006)
3. Tag each person's job using LLM + Structured Output
4. Keep only those tagged `transport`
5. POST result to `https://hub.ag3nts.org/verify`

**Available tags**: IT, transport, edukacja, medycyna, praca z ludźmi, praca z pojazdami, praca fizyczna

> **Check yourself before coding:**
> - Why use Structured Output for tagging instead of just asking the model to return JSON text?
> - Why send all job descriptions in one LLM call instead of one call per person?
> - The `born` field should be an integer. What bug would appear if you sent it as a string?
