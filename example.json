{
    "title": "Cherry-pick a lesson",
    "description": "User interactively selects and cherry-picks a lesson",
    "tested": true,
    "steps": [
      "Run `npm init -y && npm install csv-parse openai`",
      "Create `.env` with `OPENROUTER_API_KEY` and `AG3NTS_API_KEY`",
      "OpenAI SDK works with OpenRouter via base URL override"
    ]
}